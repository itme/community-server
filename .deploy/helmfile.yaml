repositories:
- name: itme
  url: https://itme.github.io/charts

environments:
  production:
  staging:

releases:
  - name: solid
    namespace: {{ .Environment.Name }}-css
    chart: itme/community-solid-server
    version: 0.1.3
    values:
     - ./values/{{ .Environment.Name}}.yaml.gotmpl
